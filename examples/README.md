# Examples

This directory contains small demo files for `paping-go`.

`localhost-demo.csv` was generated against a local test server on `127.0.0.1:8080`.

It is intended for documentation and report generation examples only.

```bash
paping-go report examples/localhost-demo.csv -o report.html
```
